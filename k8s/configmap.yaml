apiVersion: v1
kind: ConfigMap
metadata:
  name: fastify-auth-config
  labels:
    app: fastify-auth-service
data:
  NODE_ENV: "production"
  PORT: "8000"
  HOST: "0.0.0.0"
  LOG_LEVEL: "info"
  DB_TYPE: "sqlitecloud"
  LOKI_ENABLED: "true"

