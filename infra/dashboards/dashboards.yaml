apiVersion: 1

providers:
  - name: 'Fastify Auth Dashboards'
    orgId: 1
    folder: 'Fastify Auth'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
