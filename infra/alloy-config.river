
logging {
  level = "info"
  format = "logfmt"
}

prometheus.scrape "fastify_app" {
  targets = [
    { "__address__" = "host.docker.internal:9464" },
  ]
  metrics_path = "/metrics"
  scrape_interval = "15s"
  forward_to = [prometheus.remote_write.mimir.receiver]
}

prometheus.remote_write "mimir" {
  endpoint {
    url = "http://mimir:9009/api/v1/push"
  }
}
