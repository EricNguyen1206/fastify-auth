# .github/workflows/deploy-cloudrun.yml
name: Deploy to Cloud Run

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch: # Manual trigger

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: asia-southeast1
  SERVICE_NAME: auth-service
  REPOSITORY: auth-service
  REGISTRY: ${{ env.REGION }}-docker.pkg.dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # Permissions needed for Workload Identity Federation
    permissions:
      contents: read
      id-token: write

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Authenticate to Google Cloud
      # Option A: Using Workload Identity Federation (RECOMMENDED - No JSON keys)
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      # Option B: Using Service Account Key (Less secure, but simpler for testing)
      # Uncomment below and comment Option A if using service account key
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Step 3: Setup Cloud SDK
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      # Step 4: Create Artifact Registry repository (if not exists)
      - name: Create Artifact Registry repository
        run: |
          gcloud artifacts repositories describe ${{ env.REPOSITORY }} \
            --location=${{ env.REGION }} \
            --format="value(name)" || \
          gcloud artifacts repositories create ${{ env.REPOSITORY }} \
            --repository-format=docker \
            --location=${{ env.REGION }} \
            --description="Docker images for Cloud Run services" \
            --quiet

      # Step 5: Configure Docker to use Artifact Registry
      - name: Configure Docker
        run: |
          gcloud auth configure-docker ${{ env.REGISTRY }} --quiet

      # Step 6: Set image tags
      - name: Set image tags
        id: image-tags
        run: |
          IMAGE_TAG=${{ github.sha }}
          IMAGE_LATEST=latest
          IMAGE_SHORT_SHA=${IMAGE_TAG:0:7}
          
          FULL_IMAGE_TAG="${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${IMAGE_TAG}"
          FULL_IMAGE_LATEST="${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${IMAGE_LATEST}"
          FULL_IMAGE_SHORT="${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:${IMAGE_SHORT_SHA}"
          
          echo "full_tag=${FULL_IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "latest_tag=${FULL_IMAGE_LATEST}" >> $GITHUB_OUTPUT
          echo "short_tag=${FULL_IMAGE_SHORT}" >> $GITHUB_OUTPUT
          echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_OUTPUT

      # Step 7: Build and push Docker image with cache
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ steps.image-tags.outputs.full_tag }}
            ${{ steps.image-tags.outputs.latest_tag }}
            ${{ steps.image-tags.outputs.short_tag }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE_NAME }}:buildcache,mode=max
          build-args: |
            NODE_ENV=production

      # Step 8: Deploy to Cloud Run
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image ${{ steps.image-tags.outputs.full_tag }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 10 \
            --timeout 300 \
            --concurrency 80 \
            --port 8080 \
            --set-env-vars NODE_ENV=production,PORT=8080,HOST=0.0.0.0,LOG_LEVEL=info \
            --update-secrets DATABASE_URI=DATABASE_URI:latest,JWT_SECRET=JWT_SECRET:latest,COOKIE_SECRET=COOKIE_SECRET:latest \
            --quiet

      # Step 9: Show service URL and info
      - name: Show Service URL
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region ${{ env.REGION }} \
            --format 'value(status.url)')
          echo "✅ Deployment successful!"
          echo "🌐 Service URL: $SERVICE_URL"
          echo "📦 Image: ${{ steps.image-tags.outputs.full_tag }}"
          echo "🔗 Health check: $SERVICE_URL/health"
          
      # Step 10: Test health endpoint (optional)
      - name: Test health endpoint
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region ${{ env.REGION }} \
            --format 'value(status.url)')
          sleep 10  # Wait for service to be ready
          curl -f "$SERVICE_URL/health" || echo "⚠️ Health check failed, but deployment succeeded"